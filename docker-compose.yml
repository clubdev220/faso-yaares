services:
  yaare:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yaare
    restart: unless-stopped
    ports:
      - "3009:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
